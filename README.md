# hw1-Haitong.Huang

This repository contains Haitong Huang's Generative AI homework assignment, exploring how AI agents can transform healthcare delivery, clinical documentation, and patient support systems.

## Git Steps Used

1. Created a local project folder and ran `git init`
2. Created the three required files: `README.md`, `assistant_draft.md`, `final_version.md`
3. Made an initial commit to establish the repository structure
4. Used Claude Code to generate content for `assistant_draft.md`, then committed
5. Manually edited `final_version.md` and committed the revised version
6. Connected the local repo to GitHub with `git remote add origin` and pushed with `git push -u origin main`

## Commit History

```
8720d38 merge remote
1423312 assistant-created draft
95a6454 assistant-created draft (initial content)
0000132 repository setup
```

## Video Walkthrough

*(link to be added after recording)*
